allprojects {
    apply from: rootProject.file('extras.gradle')

    group GROUP
    version VERSION

    repositories {
        mavenCentral()
        mavenLocal()
        maven {
            url('https://oss.sonatype.org/content/repositories/snapshots')
        }
    }
}